with open('9-input.txt') as f:
  numbers = [r.strip() for r in f.readlines()] 

def find_pair(number, preamble):
  cur_num = int(numbers[number])
  print(cur_num)
  # for n in numbers[pream]


preamble = 5


for i, n in numbers(enumerate(range(), 5)) :
  find_pair(int(numbers[i]),5)