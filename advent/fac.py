def search_bags(color):
  search master list for color
  parse it into a list,dict
  for each_child in color
    if any_child == 'shiny gold bag'
      return true
    if child == 'no more bags'
      return false
    else:
      return search_bags(child)


